<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lawsection">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lawsection">
        <activity android:name=".AdminCase"></activity>
        <activity android:name=".AdminLaw" />
        <activity android:name=".Admin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Section" />
        <activity android:name=".criminal_law" />
        <activity android:name=".common_law" />
        <activity android:name=".civil_law" />
        <activity android:name=".CaseRegister" />
        <activity android:name=".Lawsection" />
        <activity android:name=".Home" />
        <activity android:name=".Register" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>